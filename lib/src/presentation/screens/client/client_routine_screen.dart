import 'package:flutter/material.dart';
import 'routine_selection_screen.dart';

class ClientRoutineScreen extends StatelessWidget {
  const ClientRoutineScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const RoutineSelectionScreen();
  }
}
